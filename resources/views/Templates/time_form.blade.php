<div class="form-group">
<label for="{{ $id }}" class="col-sm-{{ $label_size }} control-label">{{ $label }}</label>
	<div class="col-sm-{{ $input_size }}">
		<input type="time" name="{{ $id }}" id="{{ $id }}" value="{{ $value }}">
	</div>
</div>
